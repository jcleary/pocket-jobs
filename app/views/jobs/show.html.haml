.container
  .text-center
    .h2.mb-4.mt-3 Pocket Jobs
  .row{ style: 'height: 600px; border: 1px black' }
    - User.children.each do |child|
      .col-md-4.d-flex.flex-column.justify-content-between
        = render 'jobs_card', child: child 
        .card.mb-4.shadow-sm
          = render 'add_a_job', child: child
          = render 'progress_and_bank', child: child

