.bg-light
  .container-fluid
    .row
      .col-12
        .text-center.h2 Pocker Jobs
    .row
      - User.children.each do |child|
        .col-lg-4.col-md-12
          .card.mb-4.shadow-sm
            .card-header
              %h4.my-0.font-weight-normal=child.name
            .list-group
              = render 'jobs', child: child 
              = render 'add_a_job', child: child
              = render 'progress_and_bank', child: child


